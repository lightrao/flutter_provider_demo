import 'package:flutter/foundation.dart';

/// A provider that manages counter state
class Counter with ChangeNotifier {
  int _count = 0;

  int get count => _count;

  void increment() {
    _count++;
    notifyListeners();
  }
}